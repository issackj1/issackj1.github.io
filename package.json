{
  "name": "lit-portfolio",
  "version": "1.0.0",
  "description": "A Lit-powered portfolio on GitHub Pages",
  "main": "src/index.html",
  "scripts": {
    "build": "rimraf dist && esbuild src/index.ts --bundle --minify --outfile=dist/bundle.js --format=esm && copyfiles -u 1 public/**/* dist && copyfiles -f src/index.html dist && copyfiles -u 1 data/**/* dist/data",
    "serve": "npx serve dist",
    "dev": "wds",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [
    "lit",
    "portfolio",
    "github-pages"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^1.0.4",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.19.0",
    "gh-pages": "^6.3.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.8.3"
  }
}
